namespace PracticeIntermediate
{
    public class Vehicle
    {
        private readonly string RegNum;

        public Vehicle(string regNum)
        {
            RegNum = regNum;
            System.Console.WriteLine(RegNum);
        }

    }
}
